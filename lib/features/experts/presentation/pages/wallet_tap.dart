import 'package:flutter/material.dart';

class WalletTap extends StatelessWidget {
  const WalletTap({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
