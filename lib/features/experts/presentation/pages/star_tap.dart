import 'package:flutter/material.dart';

class StarTap extends StatelessWidget {
  const StarTap({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
